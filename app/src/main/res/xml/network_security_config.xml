<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Security Configuration (SEC3):
        - Cleartext (HTTP) traffic is BLOCKED by default for all domains
        - Public APIs (plex.tv, tmdb, omdb) MUST use HTTPS (enforced at OS level)
        - Exception: localhost connections allowed for local testing/development

        Note: Local Plex servers on private IPs (192.168.x.x, 10.x.x.x) should use HTTPS.
        The app supports self-signed certificates for LAN servers via custom TrustManager.
        Android NSC does not support IP range wildcards, so HTTP-only servers on private
        IPs require HTTPS migration or will be blocked.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext only for localhost (development/testing) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Android emulator host -->
    </domain-config>
</network-security-config>
